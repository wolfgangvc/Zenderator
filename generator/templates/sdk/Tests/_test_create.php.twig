    /**
    *
    **/
    public function test{{ route.function|capitalize }}()
    {
        ${{ route.singular }} = $this->{{ pack_name|lower }}->{{ route.function }}([
            {% for property, value in route.example %}
                '{{ property }}' => {{ var_export(value) }},
            {% endfor %}
        ]);
        return ${{ route.singular }};
    }