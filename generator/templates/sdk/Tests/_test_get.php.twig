    /**
    * @depends testList
    **/
    public function test{{ route.function|capitalize }}($listOfAll{{ route.plural }})
    {
        array_shuffle($listOfAll{{ route.plural }});
        ${{ route.singular }} = reset($listOfAll{{ route.plural }});
        $response = $this->{{ pack_name|lower }}->{{ route.function }}(
            ${{ route.singular }}->getId()
        );
    }
