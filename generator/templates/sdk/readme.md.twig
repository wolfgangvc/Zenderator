# {{ app_name }}

{% for class, class_packs in packs %}
## {{ class }}

```
    use \Segura\SDK\{{ app_name }}\Client as SDKClient;
    $sdkClient      = new SDKClient();
    ${{ class }} = $sdkClient->get{{ class }}();
```

{% endfor %}