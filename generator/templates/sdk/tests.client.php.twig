<?php
namespace Segura\SDK\{{ app_name }}\Test;

use Segura\SDK\{{ app_name }}\AccessLayer\{{ pack_name }}AccessLayer;
use Segura\SDK\{{ app_name }}\Client as SDKClient;

class {{ pack_name }}Test extends \PHPUnit_Framework_TestCase{
    /** @var SDKClient */
    private $client;

    /** @var {{ pack_name }}AccessLayer */
    private ${{ pack_name|lower }};

    public function setUp()
    {
        parent::setUp();
        $this->client = new SDKClient();
        $this->{{ pack_name|lower }} = $this->client->get{{ pack_name }}();
    }

{% for route in routes %}
    /**
     *
     **/
    public function test{{ route.function|capitalize }}()
    {
        $response = $this->{{ pack_name|lower }}->{{ route.function }}();
        $this->assertNotNull(json_decode($response));
    }

{% endfor %}
}